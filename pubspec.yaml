name: editor
description: >-
  Встраиваемый текстовый редактор для Flutter: piece tree, undo/redo,
  многослойная стилизация, gutter, inlay hints, LSP-хуки.
version: 0.0.1
publish_to: none
homepage:

environment:
  sdk: ^3.12.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  test: ^1.26.0